import { DefaultTheme } from "@react-navigation/native";

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#6096BA",
        background: "#E7ECEF",
        card: "#DFDCD7",
        text: "#050A0F",
        lightText: "#E7ECEF",
    },
};

export default theme;
