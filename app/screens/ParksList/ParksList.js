import React from 'react';
import { View, Text } from 'react-native';

const ParksList = () => (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>ParksList Screen</Text>
    </View>
);

export default ParksList;
